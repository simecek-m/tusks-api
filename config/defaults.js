// default values for environment variables
const defaults = {
  mode: 'dev',
  logLevel: 'debug',
  port: 8080,
  mongoUrl: 'mongodb://localhost/todo-db',
  ipAddress: 'localhost'
};

module.exports = defaults;
