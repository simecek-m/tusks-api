{
  "name": "todo_server",
  "version": "1.0.0",
  "description": "REST API for simple todo app",
  "main": "app.js",
  "repository": "https://github.com/simecek-m/todo_server.git",
  "author": "Martin Simecek <simecek.m@seznam.cz>",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.0.0",
    "express": "^4.17.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.5.9",
    "winston": "^3.2.1",
    "winston-daily-rotate-file": "^3.9.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "chai-json-schema": "^1.5.1",
    "codecov": "^3.5.0",
    "cross-env": "^5.2.1",
    "eslint": "^5.16.0",
    "husky": "^3.0.0",
    "link-module-alias": "^1.2.0",
    "mocha": "^6.1.4",
    "mongo-seeding": "^3.2.0-alpha.0",
    "nyc": "^14.1.1"
  },
  "scripts": {
    "postinstall": "link-module-alias",
    "start": "cross-env MODE=dev nodemon --exec \"yarn lint && node app\"",
    "lint": "eslint . --ext .js,.json",
    "lint:fix": "eslint . --ext .js,.json --fix",
    "test": "yarn lint && env MODE='test' nyc --silent mocha --exit",
    "test-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "_moduleAliases": {
    "~root": ".",
    "~middleware": "middleware",
    "~model": "model",
    "~router": "router",
    "~config": "config",
    "~schema": "test/schema",
    "~server": "server",
    "~database": "database",
    "~logger": "logger",
    "~defaults": "config/defaults.js",
    "~constants": "config/constants.js",
    "~auth": "auth"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint",
      "pre-push": "yarn lint && yarn test"
    }
  }
}
